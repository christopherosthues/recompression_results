\documentclass[10pt,a4paper]{standalone}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage{graphicx}

\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.15}
\input{../header}
\begin{document}
    % IMPORT-DATA recomp results_final/fin/memory.txt

    \begin{tikzpicture}
        \begin{axis}[
        cycle list name=mycolorlist,
        title={Vergleich des Speicherverbrauchs (fib41, 267 MB)},
        xlabel={Kerne},
        ylabel={Größe in GB},
        legend cell align=left,
        legend pos=outer north east
        ]
        %% MULTIPLOT(algo) SELECT cores AS x, (SUM(memory) * 1.0 / 1000000000) AS y, MULTIPLOT
        %% FROM recomp WHERE algo="parallel_recompression" AND dataset="fib41" GROUP BY MULTIPLOT,x ORDER BY MULTIPLOT,x
        \addplot coordinates { (1,3.13748) (2,3.13748) (4,3.13748) (6,3.127) (8,3.13749) (10,3.12176) (12,3.12701) (14,3.13225) (16,3.13749) (18,3.14274) (20,3.12176) };
        \addlegendentry{\parallelrecomp};

        %% MULTIPLOT(algo) SELECT cores AS x, (SUM(memory) * 1.0 / 1000000000) AS y, MULTIPLOT
        %% FROM recomp WHERE algo="parallel_lp_recompression" AND dataset="fib41" GROUP BY MULTIPLOT,x ORDER BY MULTIPLOT,x
        \addplot coordinates { (1,3.13748) (2,3.13748) (4,3.13748) (6,3.127) (8,3.13749) (10,3.12176) (12,3.12701) (14,3.13225) (16,3.13749) (18,3.14274) (20,3.12176) };
        \addlegendentry{\parallellprecomp};

        %% MULTIPLOT(algo) SELECT cores AS x, (SUM(memory) * 1.0 / 1000000000) AS y, MULTIPLOT
        %% FROM recomp WHERE algo="parallel_rnd_recompression" AND dataset="fib41" GROUP BY MULTIPLOT,x ORDER BY MULTIPLOT,x
        \addplot coordinates { (1,3.13748) (2,3.13748) (4,3.13748) (6,3.127) (8,3.13748) (10,3.12176) (12,3.12701) (14,3.13225) (16,3.13749) (18,3.14274) (20,3.12176) };
        \addlegendentry{\parallelrndrecomp};

        %% MULTIPLOT(algo) SELECT cores AS x, (SUM(memory) * 1.0 / 1000000000) AS y, MULTIPLOT
        %% FROM recomp WHERE algo="parallel_ls_recompression" AND dataset="fib41" GROUP BY MULTIPLOT,x ORDER BY MULTIPLOT,x
        \addplot coordinates { (1,3.13748) (2,3.13748) (4,3.13748) (6,3.127) (8,3.13748) (10,3.12176) (12,3.12701) (14,3.13225) (16,3.13749) (18,3.14274) (20,3.12176) };
        \addlegendentry{\parallellsrecomp};

        %% MULTIPLOT(algo) SELECT cores AS x, (SUM(memory) * 1.0 / 1000000000) AS y, MULTIPLOT
        %% FROM recomp WHERE algo="parallel_gr_recompression" AND dataset="fib41" GROUP BY MULTIPLOT,x ORDER BY MULTIPLOT,x
        \addplot coordinates { (1,3.13748) (2,3.13748) (4,3.13748) (6,3.127) (8,3.13748) (10,3.12176) (12,3.12701) (14,3.13225) (16,3.13749) (18,3.14274) (20,3.12176) };
        \addlegendentry{\parallelgrrecomp};

        %% MULTIPLOT(algo) SELECT cores AS x, (SUM(memory) * 1.0 / 1000000000) AS y, MULTIPLOT
        %% FROM recomp WHERE algo="parallel_rnd10_recompression" AND dataset="fib41" GROUP BY MULTIPLOT,x ORDER BY MULTIPLOT,x
        \addplot coordinates { (1,3.13748) (2,3.13748) (4,3.13748) (6,3.127) (8,3.13748) (10,3.12176) (12,3.12701) (14,3.13225) (16,3.13749) (18,3.14274) (20,3.12176) };
        \addlegendentry{\parallelrndkrecomp};

        %% MULTIPLOT(algo) SELECT cores AS x, (SUM(memory) * 1.0 / 1000000000) AS y, MULTIPLOT
        %% FROM recomp WHERE algo="parallel_rnddir10_recompression" AND dataset="fib41" GROUP BY MULTIPLOT,x ORDER BY MULTIPLOT,x
        \addplot coordinates { (1,3.13748) (2,3.13748) (4,3.13748) (6,3.127) (8,3.13748) (10,3.12176) (12,3.12701) (14,3.13225) (16,3.13749) (18,3.14274) (20,3.12176) };
        \addlegendentry{\parallelrnddirkrecomp};

        %% MULTIPLOT(algo) SELECT cores AS x, (SUM(memory) * 1.0 / 1000000000) AS y, MULTIPLOT
        %% FROM recomp WHERE algo="fast_seq_recompression" AND dataset="fib41" GROUP BY MULTIPLOT,x ORDER BY MULTIPLOT,x
        \addplot coordinates { (1,2.96408) };
        \addlegendentry{\fastseqrecomp};

        %% MULTIPLOT(algo) SELECT cores AS x, (SUM(memory) * 1.0 / 1000000000) AS y, MULTIPLOT
        %% FROM recomp WHERE algo="hash_recompression" AND dataset="fib41" GROUP BY MULTIPLOT,x ORDER BY MULTIPLOT,x
        \addplot coordinates { (1,1.89034) };
        \addlegendentry{\hashrecomp};

        %% MULTIPLOT(algo) SELECT cores AS x, (SUM(memory) * 1.0 / 1000000000) AS y, MULTIPLOT
        %% FROM recomp WHERE algo="naive_recompression" AND dataset="fib41" GROUP BY MULTIPLOT,x ORDER BY MULTIPLOT,x
        \addplot coordinates { (1,1.57707) };
        \addlegendentry{\naiverecomp};

        %% MULTIPLOT(algo) SELECT cores AS x, (SUM(memory) * 1.0 / 1000000000) AS y, MULTIPLOT
        %% FROM recomp WHERE algo="esp" AND dataset="fib41" GROUP BY MULTIPLOT,x ORDER BY MULTIPLOT,x
        \addplot coordinates { (1,1.56365) };
        \addlegendentry{\esp};

\end{axis}
    \end{tikzpicture}
\end{document}
