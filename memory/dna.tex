\documentclass[10pt,a4paper]{standalone}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage{graphicx}

\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.15}
\input{../header}
\begin{document}
    % IMPORT-DATA recomp results_final/fin/memory.txt

    \begin{tikzpicture}
        \begin{axis}[
        cycle list name=mycolorlist,
        title={Vergleich des Speicherverbrauchs (dna, 403 MB)},
        xlabel={Kerne},
        ylabel={Größe in GB},
        legend cell align=left,
        legend pos=outer north east
        ]
        %% MULTIPLOT(algo) SELECT cores AS x, (SUM(memory) * 1.0 / 1000000000) AS y, MULTIPLOT
        %% FROM recomp WHERE algo="parallel_recompression" AND dataset="dna" GROUP BY MULTIPLOT,x ORDER BY MULTIPLOT,x
        \addplot coordinates { (1,4.2018) (2,4.2018) (4,4.2018) (6,4.19132) (8,4.20181) (10,4.21229) (12,4.19132) (14,4.19657) (16,4.20181) (18,4.20706) (20,4.2123) };
        \addlegendentry{\parallelrecomp};

        %% MULTIPLOT(algo) SELECT cores AS x, (SUM(memory) * 1.0 / 1000000000) AS y, MULTIPLOT
        %% FROM recomp WHERE algo="parallel_lp_recompression" AND dataset="dna" GROUP BY MULTIPLOT,x ORDER BY MULTIPLOT,x
        \addplot coordinates { (1,4.2018) (2,4.2018) (4,4.2018) (6,4.19132) (8,4.20181) (10,4.21229) (12,4.19132) (14,4.19657) (16,4.20181) (18,4.20706) (20,4.2123) };
        \addlegendentry{\parallellprecomp};

        %% MULTIPLOT(algo) SELECT cores AS x, (SUM(memory) * 1.0 / 1000000000) AS y, MULTIPLOT
        %% FROM recomp WHERE algo="parallel_rnd_recompression" AND dataset="dna" GROUP BY MULTIPLOT,x ORDER BY MULTIPLOT,x
        \addplot coordinates { (1,4.2018) (2,4.2018) (4,4.2018) (6,4.19132) (8,4.2018) (10,4.21229) (12,4.19132) (14,4.19657) (16,4.20181) (18,4.20706) (20,4.2123) };
        \addlegendentry{\parallelrndrecomp};

        %% MULTIPLOT(algo) SELECT cores AS x, (SUM(memory) * 1.0 / 1000000000) AS y, MULTIPLOT
        %% FROM recomp WHERE algo="parallel_ls_recompression" AND dataset="dna" GROUP BY MULTIPLOT,x ORDER BY MULTIPLOT,x
        \addplot coordinates { (1,4.2018) (2,4.2018) (4,4.2018) (6,4.19132) (8,4.2018) (10,4.21229) (12,4.19132) (14,4.19657) (16,4.20181) (18,4.20706) (20,4.2123) };
        \addlegendentry{\parallellsrecomp};

        %% MULTIPLOT(algo) SELECT cores AS x, (SUM(memory) * 1.0 / 1000000000) AS y, MULTIPLOT
        %% FROM recomp WHERE algo="parallel_gr_recompression" AND dataset="dna" GROUP BY MULTIPLOT,x ORDER BY MULTIPLOT,x
        \addplot coordinates { (1,4.2018) (2,4.2018) (4,4.2018) (6,4.19132) (8,4.2018) (10,4.21229) (12,4.19132) (14,4.19657) (16,4.20181) (18,4.20706) (20,4.2123) };
        \addlegendentry{\parallelgrrecomp};

        %% MULTIPLOT(algo) SELECT cores AS x, (SUM(memory) * 1.0 / 1000000000) AS y, MULTIPLOT
        %% FROM recomp WHERE algo="parallel_rnd10_recompression" AND dataset="dna" GROUP BY MULTIPLOT,x ORDER BY MULTIPLOT,x
        \addplot coordinates { (1,4.2018) (2,4.2018) (4,4.2018) (6,4.19132) (8,4.2018) (10,4.21229) (12,4.19132) (14,4.19657) (16,4.20181) (18,4.20706) (20,4.2123) };
        \addlegendentry{\parallelrndkrecomp};

        %% MULTIPLOT(algo) SELECT cores AS x, (SUM(memory) * 1.0 / 1000000000) AS y, MULTIPLOT
        %% FROM recomp WHERE algo="parallel_rnddir10_recompression" AND dataset="dna" GROUP BY MULTIPLOT,x ORDER BY MULTIPLOT,x
        \addplot coordinates { (1,4.2018) (2,4.2018) (4,4.2018) (6,4.19132) (8,4.2018) (10,4.21229) (12,4.19132) (14,4.19657) (16,4.20181) (18,4.20706) (20,4.2123) };
        \addlegendentry{\parallelrnddirkrecomp};

        %% MULTIPLOT(algo) SELECT cores AS x, (SUM(memory) * 1.0 / 1000000000) AS y, MULTIPLOT
        %% FROM recomp WHERE algo="fast_seq_recompression" AND dataset="dna" GROUP BY MULTIPLOT,x ORDER BY MULTIPLOT,x
        \addplot coordinates { (1,4.89566) };
        \addlegendentry{\fastseqrecomp};

        %% MULTIPLOT(algo) SELECT cores AS x, (SUM(memory) * 1.0 / 1000000000) AS y, MULTIPLOT
        %% FROM recomp WHERE algo="hash_recompression" AND dataset="dna" GROUP BY MULTIPLOT,x ORDER BY MULTIPLOT,x
        \addplot coordinates { (1,2.74818) };
        \addlegendentry{\hashrecomp};

        %% MULTIPLOT(algo) SELECT cores AS x, (SUM(memory) * 1.0 / 1000000000) AS y, MULTIPLOT
        %% FROM recomp WHERE algo="naive_recompression" AND dataset="dna" GROUP BY MULTIPLOT,x ORDER BY MULTIPLOT,x
        \addplot coordinates { (1,4.64128) };
        \addlegendentry{\naiverecomp};

        %% MULTIPLOT(algo) SELECT cores AS x, (SUM(memory) * 1.0 / 1000000000) AS y, MULTIPLOT
        %% FROM recomp WHERE algo="esp" AND dataset="dna" GROUP BY MULTIPLOT,x ORDER BY MULTIPLOT,x
        \addplot coordinates { (1,2.04793) };
        \addlegendentry{\esp};

\end{axis}
    \end{tikzpicture}
\end{document}
