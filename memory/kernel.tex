\documentclass[10pt,a4paper]{standalone}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage{graphicx}

\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.15}
\input{../header}
\begin{document}
    % IMPORT-DATA recomp results_final/fin/memory.txt

    \begin{tikzpicture}
        \begin{axis}[
        cycle list name=mycolorlist,
        title={Vergleich des Speicherverbrauchs (kernel, 257 MB)},
        xlabel={Kerne},
        ylabel={Größe in GB},
        legend cell align=left,
        legend pos=outer north east
        ]
        %% MULTIPLOT(algo) SELECT cores AS x, (SUM(memory) * 1.0 / 1000000000) AS y, MULTIPLOT
        %% FROM recomp WHERE algo="parallel_recompression" AND dataset="kernel" GROUP BY MULTIPLOT,x ORDER BY MULTIPLOT,x
        \addplot coordinates { (1,2.82916) (2,2.83139) (4,2.83567) (6,2.83995) (8,2.84423) (10,2.84852) (12,2.8528) (14,2.85708) (16,2.86136) (18,2.86564) (20,2.86992) };
        \addlegendentry{\parallelrecomp};

        %% MULTIPLOT(algo) SELECT cores AS x, (SUM(memory) * 1.0 / 1000000000) AS y, MULTIPLOT
        %% FROM recomp WHERE algo="parallel_lp_recompression" AND dataset="kernel" GROUP BY MULTIPLOT,x ORDER BY MULTIPLOT,x
        \addplot coordinates { (1,2.82916) (2,2.83139) (4,2.83567) (6,2.83995) (8,2.84423) (10,2.84852) (12,2.8528) (14,2.85708) (16,2.86136) (18,2.86564) (20,2.86992) };
        \addlegendentry{\parallellprecomp};

        %% MULTIPLOT(algo) SELECT cores AS x, (SUM(memory) * 1.0 / 1000000000) AS y, MULTIPLOT
        %% FROM recomp WHERE algo="parallel_rnd_recompression" AND dataset="kernel" GROUP BY MULTIPLOT,x ORDER BY MULTIPLOT,x
        \addplot coordinates { (1,2.82916) (2,2.83139) (4,2.83567) (6,2.83995) (8,2.84423) (10,2.84851) (12,2.85279) (14,2.85708) (16,2.86136) (18,2.86564) (20,2.86992) };
        \addlegendentry{\parallelrndrecomp};

        %% MULTIPLOT(algo) SELECT cores AS x, (SUM(memory) * 1.0 / 1000000000) AS y, MULTIPLOT
        %% FROM recomp WHERE algo="parallel_ls_recompression" AND dataset="kernel" GROUP BY MULTIPLOT,x ORDER BY MULTIPLOT,x
        \addplot coordinates { (1,2.82916) (2,2.83139) (4,2.83567) (6,2.83995) (8,2.84423) (10,2.84852) (12,2.8528) (14,2.85708) (16,2.86136) (18,2.86564) (20,2.86992) };
        \addlegendentry{\parallellsrecomp};

        %% MULTIPLOT(algo) SELECT cores AS x, (SUM(memory) * 1.0 / 1000000000) AS y, MULTIPLOT
        %% FROM recomp WHERE algo="parallel_gr_recompression" AND dataset="kernel" GROUP BY MULTIPLOT,x ORDER BY MULTIPLOT,x
        \addplot coordinates { (1,2.82916) (2,2.83139) (4,2.83567) (6,2.83995) (8,2.84423) (10,2.84851) (12,2.85279) (14,2.85708) (16,2.86136) (18,2.86564) (20,2.86992) };
        \addlegendentry{\parallelgrrecomp};

        %% MULTIPLOT(algo) SELECT cores AS x, (SUM(memory) * 1.0 / 1000000000) AS y, MULTIPLOT
        %% FROM recomp WHERE algo="parallel_rnd10_recompression" AND dataset="kernel" GROUP BY MULTIPLOT,x ORDER BY MULTIPLOT,x
        \addplot coordinates { (1,2.82916) (2,2.83139) (4,2.83567) (6,2.83995) (8,2.84423) (10,2.84851) (12,2.85279) (14,2.85708) (16,2.86136) (18,2.86564) (20,2.86992) };
        \addlegendentry{\parallelrndkrecomp};

        %% MULTIPLOT(algo) SELECT cores AS x, (SUM(memory) * 1.0 / 1000000000) AS y, MULTIPLOT
        %% FROM recomp WHERE algo="parallel_rnddir10_recompression" AND dataset="kernel" GROUP BY MULTIPLOT,x ORDER BY MULTIPLOT,x
        \addplot coordinates { (1,2.82916) (2,2.83139) (4,2.83567) (6,2.83995) (8,2.84423) (10,2.84851) (12,2.8528) (14,2.85708) (16,2.86136) (18,2.86564) (20,2.86992) };
        \addlegendentry{\parallelrnddirkrecomp};

        %% MULTIPLOT(algo) SELECT cores AS x, (SUM(memory) * 1.0 / 1000000000) AS y, MULTIPLOT
        %% FROM recomp WHERE algo="fast_seq_recompression" AND dataset="kernel" GROUP BY MULTIPLOT,x ORDER BY MULTIPLOT,x
        \addplot coordinates { (1,2.24267) };
        \addlegendentry{\fastseqrecomp};

        %% MULTIPLOT(algo) SELECT cores AS x, (SUM(memory) * 1.0 / 1000000000) AS y, MULTIPLOT
        %% FROM recomp WHERE algo="hash_recompression" AND dataset="kernel" GROUP BY MULTIPLOT,x ORDER BY MULTIPLOT,x
        \addplot coordinates { (1,1.97422) };
        \addlegendentry{\hashrecomp};

        %% MULTIPLOT(algo) SELECT cores AS x, (SUM(memory) * 1.0 / 1000000000) AS y, MULTIPLOT
        %% FROM recomp WHERE algo="naive_recompression" AND dataset="kernel" GROUP BY MULTIPLOT,x ORDER BY MULTIPLOT,x
        \addplot coordinates { (1,1.64144) };
        \addlegendentry{\naiverecomp};

        %% MULTIPLOT(algo) SELECT cores AS x, (SUM(memory) * 1.0 / 1000000000) AS y, MULTIPLOT
        %% FROM recomp WHERE algo="esp" AND dataset="kernel" GROUP BY MULTIPLOT,x ORDER BY MULTIPLOT,x
        \addplot coordinates { (1,1.11206) };
        \addlegendentry{\esp};

\end{axis}
    \end{tikzpicture}
\end{document}
