\documentclass[10pt,a4paper]{standalone}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage{graphicx}

\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.15}
\input{../header}
\begin{document}

    % IMPORT-DATA ra results_final/random_access_filter.txt

    \begin{tikzpicture}
        \begin{axis}[
        cycle list name=mycolorlist,
        title={Laufzeit Random Access (coreutils, 3 Iterationen, 205 MB)},
        xlabel={Anfragen in Mio.},
        ylabel={Zeit in sec},
        legend pos=north west
        ]
        %% MULTIPLOT(algo) SELECT accesses / 1000000 AS x, (SUM(time) / 3.0 / 1000) AS y, MULTIPLOT
        %% FROM ra WHERE algo="recompression" AND dataset="coreutils" GROUP BY MULTIPLOT,x ORDER BY MULTIPLOT,x
        \addplot coordinates { (5,9.05333) (10,18.107) (15,27.1203) (20,36.119) (25,45.1407) (30,54.3193) (35,63.189) (40,72.2753) (45,81.2337) (50,90.396) };
        \addlegendentry{recompression};

        %% MULTIPLOT(algo) SELECT accesses / 1000000 AS x, (SUM(time) / 3.0 / 1000) AS y, MULTIPLOT
        %% FROM ra WHERE algo="prezza" AND dataset="coreutils" GROUP BY MULTIPLOT,x ORDER BY MULTIPLOT,x
        \addplot coordinates { (5,1.024) (10,2.042) (15,3.062) (20,4.083) (25,5.10367) (30,6.12467) (35,7.144) (40,8.16567) (45,9.18567) (50,10.2073) };
        \addlegendentry{prezza};

        %% MULTIPLOT(algo) SELECT accesses / 1000000 AS x, (SUM(time) / 3.0 / 1000) AS y, MULTIPLOT
        %% FROM ra WHERE algo="rmq" AND dataset="coreutils" GROUP BY MULTIPLOT,x ORDER BY MULTIPLOT,x

        %% MULTIPLOT(algo) SELECT accesses / 1000000 AS x, (SUM(time) / 3.0 / 1000) AS y, MULTIPLOT
        %% FROM ra WHERE algo="naive" AND dataset="coreutils" GROUP BY MULTIPLOT,x ORDER BY MULTIPLOT,x
        \addplot coordinates { (5,0.766) (10,1.53033) (15,2.29133) (20,3.054) (25,3.812) (30,4.57667) (35,5.33267) (40,6.096) (45,6.86133) (50,7.622) };
        \addlegendentry{naive};

\end{axis}
    \end{tikzpicture}
\end{document}
