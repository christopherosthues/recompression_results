\documentclass[10pt,a4paper]{standalone}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage{graphicx}

\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.15}
\input{../header}
\begin{document}
    % IMPORT-DATA ra results_final/random_access_filter.txt

    \begin{tikzpicture}
        \begin{axis}[
        cycle list name=mycolorlist,
        title={Laufzeit Random Access (english, 3 Iterationen, 2210 MB)},
        xlabel={Anfragen in Mio.},
        ylabel={Zeit in sec},
        legend pos=north west
        ]
        %% MULTIPLOT(algo) SELECT accesses / 1000000 AS x, (SUM(time) / 3.0 / 1000) AS y, MULTIPLOT
        %% FROM ra WHERE algo="recompression" AND dataset="english" GROUP BY MULTIPLOT,x ORDER BY MULTIPLOT,x
        \addplot coordinates { (5,15.1877) (10,30.3643) (15,45.541) (20,60.7293) (25,75.908) (30,91.0773) (35,106.267) (40,121.446) (45,136.616) (50,151.795) };
        \addlegendentry{recompression};

        %% MULTIPLOT(algo) SELECT accesses / 1000000 AS x, (SUM(time) / 3.0 / 1000) AS y, MULTIPLOT
        %% FROM ra WHERE algo="prezza" AND dataset="english" GROUP BY MULTIPLOT,x ORDER BY MULTIPLOT,x
        \addplot coordinates { (5,1.062) (10,2.119) (15,3.176) (20,4.23333) (25,5.29067) (30,6.34733) (35,7.40433) (40,8.46033) (45,9.519) (50,10.5743) };
        \addlegendentry{prezza};

        %% MULTIPLOT(algo) SELECT accesses / 1000000 AS x, (SUM(time) / 3.0 / 1000) AS y, MULTIPLOT
        %% FROM ra WHERE algo="rmq" AND dataset="english" GROUP BY MULTIPLOT,x ORDER BY MULTIPLOT,x

        %% MULTIPLOT(algo) SELECT accesses / 1000000 AS x, (SUM(time) / 3.0 / 1000) AS y, MULTIPLOT
        %% FROM ra WHERE algo="naive" AND dataset="english" GROUP BY MULTIPLOT,x ORDER BY MULTIPLOT,x
        \addplot coordinates { (5,0.812) (10,1.61933) (15,2.42667) (20,3.234) (25,4.04167) (30,4.84833) (35,5.656) (40,6.46333) (45,7.271) (50,8.07733) };
        \addlegendentry{naive};

\end{axis}
    \end{tikzpicture}
\end{document}
