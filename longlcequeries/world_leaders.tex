\documentclass[10pt,a4paper]{standalone}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage{graphicx}

\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.15}
\input{../header}
\begin{document}

    % IMPORT-DATA ra results_final/long_lce_queries_filter.txt

    \begin{tikzpicture}
        \begin{axis}[
        cycle list name=mycolorlist,
        title={Laufzeit lange LCE-Queries (world\_leaders, 3 Iterationen, 46 MB)},
        xlabel={Anfragen in Mio.},
        ylabel={Zeit in sec},
        legend pos=north west
        ]
        %% MULTIPLOT(algo) SELECT queries / 1000000 AS x, (SUM(time) / 3.0 / 1000) AS y, MULTIPLOT
        %% FROM ra WHERE algo="recompression" AND dataset="world\_leaders" GROUP BY MULTIPLOT,x ORDER BY MULTIPLOT,x
        \addplot coordinates { (5,70.152) (10,141.428) (15,212.999) (20,281.164) (25,348.696) (30,414.229) (35,480.301) (40,544.36) (45,611.286) };
        \addlegendentry{recompression};

        %% MULTIPLOT(algo) SELECT queries / 1000000 AS x, (SUM(time) / 3.0 / 1000) AS y, MULTIPLOT
        %% FROM ra WHERE algo="prezza" AND dataset="world\_leaders" GROUP BY MULTIPLOT,x ORDER BY MULTIPLOT,x
        \addplot coordinates { (5,76.1773) (10,141.1) (15,201.728) (20,259.528) (25,314.152) (30,366.223) (35,413.596) (40,455.697) (45,490.745) };
        \addlegendentry{prezza};

        %% MULTIPLOT(algo) SELECT queries / 1000000 AS x, (SUM(time) / 3.0 / 1000) AS y, MULTIPLOT
        %% FROM ra WHERE algo="rmq" AND dataset="world\_leaders" GROUP BY MULTIPLOT,x ORDER BY MULTIPLOT,x
        \addplot coordinates { (5,25.1903) (10,52.849) (15,78.833) (20,106.244) (25,132.342) (30,156.5) (35,187.272) (40,216.191) (45,242.679) };
        \addlegendentry{rmq};

        %% MULTIPLOT(algo) SELECT queries / 1000000 AS x, (SUM(time) / 3.0 / 1000) AS y, MULTIPLOT
        %% FROM ra WHERE algo="naive" AND dataset="world\_leaders" GROUP BY MULTIPLOT,x ORDER BY MULTIPLOT,x
        \addplot coordinates { (5,246.636) (10,268.779) (15,281.295) (20,289.732) (25,295.631) (30,300.482) (35,302.132) (40,303.42) (45,304.859) };
        \addlegendentry{naive};

\end{axis}
    \end{tikzpicture}
\end{document}
