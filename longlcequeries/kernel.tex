\documentclass[10pt,a4paper]{standalone}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage{graphicx}

\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.15}
\input{../header}
\begin{document}

    % IMPORT-DATA ra results_final/long_lce_queries_filter.txt

    \begin{tikzpicture}
        \begin{axis}[
        cycle list name=mycolorlist,
        title={Laufzeit lange LCE-Queries (kernel, 3 Iterationen, 257 MB)},
        xlabel={Anfragen in Mio.},
        ylabel={Zeit in sec},
        legend pos=north west
        ]
        %% MULTIPLOT(algo) SELECT queries / 1000000 AS x, (SUM(time) / 3.0 / 1000) AS y, MULTIPLOT
        %% FROM ra WHERE algo="recompression" AND dataset="kernel" GROUP BY MULTIPLOT,x ORDER BY MULTIPLOT,x
        \addplot coordinates { (5,78.0593) (10,160.7) (15,246.418) (20,330.312) (25,417.304) (30,504.451) (35,590.306) (40,680.282) (45,763.433) (50,849.448) };
        \addlegendentry{recompression};

        %% MULTIPLOT(algo) SELECT queries / 1000000 AS x, (SUM(time) / 3.0 / 1000) AS y, MULTIPLOT
        %% FROM ra WHERE algo="prezza" AND dataset="kernel" GROUP BY MULTIPLOT,x ORDER BY MULTIPLOT,x
        \addplot coordinates { (5,104.871) (10,206.088) (15,306.904) (20,407.477) (25,507.516) (30,606.728) (35,705.359) (40,803.643) (45,901.721) (50,997.797) };
        \addlegendentry{prezza};

        %% MULTIPLOT(algo) SELECT queries / 1000000 AS x, (SUM(time) / 3.0 / 1000) AS y, MULTIPLOT
        %% FROM ra WHERE algo="rmq" AND dataset="kernel" GROUP BY MULTIPLOT,x ORDER BY MULTIPLOT,x
        \addplot coordinates { (5,19.5483) (10,44.9303) (15,79.507) (20,109.447) (25,144.123) (30,178.905) (35,213.464) (40,248.101) (45,293.961) (50,329.96) };
        \addlegendentry{rmq};

        %% MULTIPLOT(algo) SELECT queries / 1000000 AS x, (SUM(time) / 3.0 / 1000) AS y, MULTIPLOT
        %% FROM ra WHERE algo="naive" AND dataset="kernel" GROUP BY MULTIPLOT,x ORDER BY MULTIPLOT,x
        \addplot coordinates { (5,6063.23) (10,9506.43) (15,12150.7) (20,14347.9) (25,16221.71) };
        \addlegendentry{naive};

\end{axis}
    \end{tikzpicture}
\end{document}
